<% provide(:title, 'Create work') %>
<h1>Create work</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@work) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create work", class: "btn btn-lg btn-primary" %>
    <% end %>
    <div class="work-page-nav">
      <%= link_to 'Back', root_path %>
    </div>
  </div>
</div>
